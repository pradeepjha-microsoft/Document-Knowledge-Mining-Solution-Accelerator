import { jest } from "@jest/globals";

const useDropzone = jest.fn()
// .mockReturnValue({
//     getRootProps: jest.fn().mockReturnValue({ "data-testid": "dropzone-root" }),
//     getInputProps: jest.fn().mockReturnValue({ "data-testid": "dropzone-input" }),
//     open: jest.fn(),
//     acceptedFiles: [],
// });

// export default useDropzone;
// export default { useDropzone };
export default  useDropzone   

